{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm install --legacy-peer-deps --production=false --prefer-offline --no-audit --no-fund",
  "framework": "nextjs",
  "env": {
    "NODE_OPTIONS": "--max-old-space-size=3072",
    "NODE_PG_FORCE_NATIVE": "0",
    "SKIP_ENV_VALIDATION": "1"
  },
  "functions": {
    "app/**/*.tsx": {
      "memory": 1024,
      "maxDuration": 10
    }
  }
}
